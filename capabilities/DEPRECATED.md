# capabilities/ — DEPRECATED

**Status:** This layer is NOT used by working code.

These files were created on Day 0 as aspirational architecture but were
never tested or integrated with real hardware. The actual capabilities
are in `skills/`.

**Use instead:**
- `skills/strafe_nav.py` — Navigation
- `skills/block_detect.py` — Block detection
- `skills/centering.py` — Target centering
- `lib/movement.py` — Calibrated movement

**Do NOT build on these files.** They will be removed in a future cleanup.
